class Account {
  constructor (id, email, id_store, name, tel, type, status) {
    this.id = id
    this.email = email
    this.id_store = id_store
    this.name = name
    this.tel = tel
    this.type = type
    this.status = status
  }
}

module.exports = Account
